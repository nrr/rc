#! /usr/bin/env python

from random import randrange
from sys import argv

if __name__ == "__main__":
	try:
		host_name = argv[1]
	except:
		host_name = "unknown"
	print "%s-%s" % (host_name, randrange(65536))
