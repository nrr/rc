#! /usr/bin/env bash

set -u
set -e
set -o pipefail

SSH_ASKPASS=$HOME/j/sciolist/bin/ssh-askpass
KEYCHAIN=$(which keychain)

if [ -x "$KEYCHAIN" ]
then
	"$KEYCHAIN" `find -L ~/.ssh/keys '(' -type f -o -type l ')' -iname 'id_rsa'` ~/.ssh/id_rsa
fi
